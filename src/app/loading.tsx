"use client";

import { useEffect } from "react";
import { useLoading } from "@/context/loading-provider";

export default function Loading() {
  // Return null because the loading is already handled by the LoadingProvider
  return null;
}
